const { Validator } = require("./lib/validator"); // Add this line to export the Validator class
const { autoTrim, normalizeEmail, validateEmail } = require("./lib/validator");

module.exports = {
  Validator, // Export the Validator class
  autoTrim,
  normalizeEmail,
  validateEmail,
  // other validation functions...
};